// Package redis integrates Redis clients, typed caches, and distributed locks with Vine.
// redis+memory://name[/dbIndex] selects an ephemeral instance and client pool shared by
// components with the same instance name and database index in the process, in any application mode.
package redis
